diff --git a/node_modules/react-native-screens/ios/bottom-tabs/screen/RNSTabsScreenViewController.mm b/node_modules/react-native-screens/ios/bottom-tabs/screen/RNSTabsScreenViewController.mm
index ba94d37..c43f264 100644
--- a/node_modules/react-native-screens/ios/bottom-tabs/screen/RNSTabsScreenViewController.mm
+++ b/node_modules/react-native-screens/ios/bottom-tabs/screen/RNSTabsScreenViewController.mm
@@ -6,6 +6,22 @@
 
 @implementation RNSTabsScreenViewController
 
+- (nullable UIScrollView *)contentScrollViewForEdge:(NSDirectionalRectEdge)edge
+{
+  UIScrollView *configuredScrollView = [super contentScrollViewForEdge:edge];
+  if (edge != NSDirectionalRectEdgeBottom) {
+    return configuredScrollView;
+  }
+
+  if (configuredScrollView != nil &&
+      (configuredScrollView.alwaysBounceVertical ||
+       configuredScrollView.contentSize.height > configuredScrollView.bounds.size.height + 1.0)) {
+    return configuredScrollView;
+  }
+
+  return [RNSScrollViewFinder findScrollViewBreadthFirstFrom:self.view];
+}
+
 - (nullable RNSTabBarController *)findTabBarController
 {
   return static_cast<RNSTabBarController *_Nullable>(self.tabBarController);
diff --git a/node_modules/react-native-screens/ios/helpers/scroll-view/RNSScrollViewFinder.h b/node_modules/react-native-screens/ios/helpers/scroll-view/RNSScrollViewFinder.h
index 04d40b3..81d1324 100644
--- a/node_modules/react-native-screens/ios/helpers/scroll-view/RNSScrollViewFinder.h
+++ b/node_modules/react-native-screens/ios/helpers/scroll-view/RNSScrollViewFinder.h
@@ -15,6 +15,12 @@
  */
 + (nullable UIScrollView *)findScrollViewInFirstDescendantChainFrom:(nullable UIView *)view;
 
+/**
+ * Searches all descendants breadth-first and returns the first vertically scrollable UIScrollView.
+ * This is used for tab content that is wrapped in pagers or other native containers.
+ */
++ (nullable UIScrollView *)findScrollViewBreadthFirstFrom:(nullable UIView *)view;
+
 /**
  * Looks for UIScrollView in a similar way to `findScrollViewInFirstDescendantChainFrom`, until it finds
  * `RNSContentScrollViewProviding`. Then, it delegates the task to the provider, and returns the results. This can
diff --git a/node_modules/react-native-screens/ios/helpers/scroll-view/RNSScrollViewFinder.mm b/node_modules/react-native-screens/ios/helpers/scroll-view/RNSScrollViewFinder.mm
index 2434c61..21d0629 100644
--- a/node_modules/react-native-screens/ios/helpers/scroll-view/RNSScrollViewFinder.mm
+++ b/node_modules/react-native-screens/ios/helpers/scroll-view/RNSScrollViewFinder.mm
@@ -19,6 +19,35 @@ + (UIScrollView *)findScrollViewInFirstDescendantChainFrom:(UIView *)view
   return nil;
 }
 
++ (nullable UIScrollView *)findScrollViewBreadthFirstFrom:(nullable UIView *)view
+{
+  if (view == nil) {
+    return nil;
+  }
+
+  NSMutableArray<UIView *> *queue = [NSMutableArray arrayWithObject:view];
+  NSUInteger cursor = 0;
+  const NSUInteger maxVisitedViews = 2000;
+
+  while (cursor < queue.count && cursor < maxVisitedViews) {
+    UIView *currentView = queue[cursor++];
+
+    if ([currentView isKindOfClass:UIScrollView.class]) {
+      UIScrollView *scrollView = static_cast<UIScrollView *>(currentView);
+      BOOL hasVerticalContent =
+          scrollView.alwaysBounceVertical ||
+          scrollView.contentSize.height > scrollView.bounds.size.height + 1.0;
+      if (hasVerticalContent) {
+        return scrollView;
+      }
+    }
+
+    [queue addObjectsFromArray:currentView.subviews];
+  }
+
+  return nil;
+}
+
 + (nullable UIScrollView *)findContentScrollViewWithDelegatingToProvider:(nullable UIView *)view
 {
   UIView *currentView = view;
